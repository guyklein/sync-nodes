const appData = {
    // host: `${url}:${process.env.SERVER_1_PORT}`,
    host: null,
    // date: (new Date(Date.now())).toUTCString(),
    data: null,
};

const state = {
    data: appData,
};

module.exports = state;
